apiVersion: v1
kind : ResourceQuota
metadata:
  name: namespace-quota
  namespace: syncwaves
spec: 
  hard:
    pods: "2"
    requests.cpu: "500m"
    requests.memory: "512Mi"
    limits.cpu: "1"
    limits.memory: "1Gi"

